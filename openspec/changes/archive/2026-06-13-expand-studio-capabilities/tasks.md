# Tasks: 核心能力扩展

## 1. 图片无损优化（需求9）
- [x] 1.1 新增 `internal/imageopt`：PNG 真无损优化（重编码 + 去冗余块，像素不变），非 PNG 原样返回，失败回退原图
- [x] 1.2 生图/裁剪产物落库前按 `lossless`（默认 true）应用优化
- [x] 1.3 下载/打包按 `lossless` 应用优化
- [x] 1.4 单测：像素一致、体积不增、非 PNG 跳过、失败回退

## 2. 工作区/上下文清理与单图移除（需求4、5、6）
- [x] 2.1 `store`：新增按 id 删除资产、按会话批量删除资产
- [x] 2.2 `workspace`：移除单图端点（删记录 + 文件，会话作用域）
- [x] 2.3 `workspace`：清空工作区端点（全部资产 + 未完成任务占位）
- [x] 2.4 `agent.Orchestrator`：重置会话 Window 方法（保留 system 提示）+ 触发端点
- [x] 2.5 前端：单图移除、清空工作区（二次确认）、清理上下文（二次确认）入口
- [x] 2.6 单测：删除会话作用域隔离、清空后为空、上下文重置保留 system

## 3. 下一步预测与自动补全（需求7）
- [x] 3.1 前端：依据本地历史动作 + 偏好关键词 + 工作区状态生成 1–3 条提示
- [x] 3.2 前端：无记录时不展示；点击提示快速发起对应操作

## 4. 多参考图生图（需求2）
- [x] 4.1 `generation`：`GenerateParams` 增 `ReferenceAssetIDs []string`（≤6，超出截断），`Request` 增 `ReferenceImages [][]byte`
- [x] 4.2 主参考图驱动调色板与尺寸继承；其余作附加参考；provider 不支持多图时降级为主图 + prompt 描述
- [x] 4.3 agent 生图工具入参支持参考图 id 列表（向后兼容单图）
- [x] 4.4 前端：参考图多选（≤6）并发起生图
- [x] 4.5 单测：多图传递、超 6 截断、单图兼容、降级路径

## 5. 图生视频（需求3）
- [x] 5.1 `config`：图生视频供应商配置（happyhorse-1.0-r2v，可降级未配置）
- [x] 5.2 新增 `internal/video`：异步 task + SSE 进度 + 视频资产回填（kind=video, mime=video/mp4）
- [x] 5.3 动作描述经注入防护的结构化 slot 组装提示
- [x] 5.4 agent 工具 `image_to_video` + 白名单激活「生视频」+ prompt 更新
- [x] 5.5 前端：视频产物可播放 + 下载
- [x] 5.6 单测：任务产出视频资产、供应商未配置降级、注入防护

## 6. 游戏素材爬取（需求1）
- [x] 6.1 `config`：素材源配置（可降级未配置）
- [x] 6.2 新增 `internal/crawl`：可插拔 source 接口，按游戏名搜索 + 抓取预览，异步 task + SSE，标注来源
- [x] 6.3 无结果/部分失败明确反馈、按条跳过
- [x] 6.4 agent 工具 `crawl_game_assets` + 白名单激活「物料爬取」+ prompt 更新
- [x] 6.5 前端：爬取结果作为资产回填工作区（标注来源）
- [x] 6.6 单测：source 搜索/抓取桩、无结果反馈、部分失败跳过

## 7. 裁剪流程改造（追加）
- [x] 7a.1 crop 新增直连端点 `POST /api/session/{id}/crop`（纯处理、回填、返回尺寸/渠道，带 lossless）
- [x] 7a.2 前端 capsule 确认改直调端点（不经 agent）
- [x] 7a.3 产出 ≤6 推工作区，>6 按渠道/尺寸分目录打包下载 + 前置提示
- [x] 7a.4 资产卡展示尺寸标识（宽×高）
- [x] 7a.5 工作区任务按状态阶段里程碑式分组（进行中/已完成/失败）
- [x] 7a.6 端到端验证：直调回填、超 6 打包分目录、卡尺寸、任务分组

## 8. 验证
- [x] 8.1 `go build ./...` + `go test ./...` 全绿
- [x] 8.2 浏览器端到端回归：自动补全、清理/移除、多参考图、压缩开关、视频播放占位
- [ ] 8.3 爬取/图生视频在配齐 provider 的环境单独验证（待办：本环境 provider 未配置，仅验证了降级路径；需在配齐 CRAWL_ENDPOINT / HAPPYHORSE_* 的环境跑真实端到端）
- [x] 8.4 `openspec validate expand-studio-capabilities --strict` 通过
