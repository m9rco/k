# Tasks — Polish Studio UI

## 1. 前端模块化与设计 token（基础重构）
- [x] 1.1 抽出 `styles.css` 的 `:root` 设计 token 分区（颜色、圆角、间距、动效 duration/easing），统一引用
- [~] 1.2 保持原生 ESM 单文件，按职责清晰分区（未做物理多文件拆分——无 spec 验收基准且回归风险高，详见交付说明）
- [x] 1.3 回归验证：上传→生图→裁剪→打包、清上下文、二次调整等现有交互全部跑通无回归

## 2. 科技感动效（需求：品牌化科技感界面 / 交互流畅度）
- [x] 2.1 品牌区/空状态加入内联 SVG + CSS 动画（描边流动/脉冲），体量小、无额外资源请求
- [x] 2.2 按钮、卡片、弹层补充微交互过渡（悬停/按下/加载态），仅用 transform/opacity
- [x] 2.3 流式滚动锚定与加载过渡复核，消除抖动/空白闪烁（沿用既有锚定逻辑，未回归）
- [x] 2.4 实现 `@media (prefers-reduced-motion: reduce)` 降级，关闭非必要动画
- [x] 2.5 验证：开/关系统「减少动态效果」两种情形下视觉与可用性均正常

## 3. 渠道视觉标识（需求：渠道视觉标识）
- [x] 3.1 建立前端 `channelId → 品牌色` 映射（覆盖 27 个渠道），缺省按分组回退默认色
- [x] 3.2 在尺寸选择器渠道列表项渲染「品牌色色块 + 首字/首字母」标识
- [x] 3.3 验证：所有渠道均有标识、无缺图、无控制台报错

## 4. 单图直接查看入口（需求：单图直接查看入口）
- [x] 4.1 资产卡新增独立「查看」小按钮，`stopPropagation` 后直接 `openLightbox(asset)`
- [x] 4.2 验证：点「查看」打开预览且不改变多选；点卡片主体仍切换多选

## 5. 工作区拖拽排序 + 多列分栏（需求：工作区拖拽排序与多列分栏）
- [x] 5.1 「已完成」阶段多列响应式网格（沿用 auto-fill minmax），窄屏退化为少列/单列
- [x] 5.2 用原生 HTML Drag and Drop 实现已完成资产拖拽调序，顺序存于前端 `state.order`，`renderWorkspace` 按其排序
- [x] 5.3 确认顺序不持久化到后端，刷新后回退后端默认顺序（newest-first）
- [x] 5.4 验证：拖拽后顺序即时更新；刷新后回退默认；窄屏分栏退化正常

## 6. 路线图视觉降权（需求：未来功能路线图展示）
- [x] 6.1 将「规划中的能力」改为 `<details>` 可折叠的低权重呈现，默认收起，释放工作区主视觉
- [x] 6.2 验证：路线图仍可见、仍标注「规划中」、仍不触发后端；工作区资产为主视觉焦点

## 7. 提示词优化端点（后端，需求：提示词优化端点）
- [x] 7.1 在 `internal/agent/optimize.go` 增加 `OptimizePrompt(ctx, text)`：用会话理解模型按独立改写指令产出生图提示词，不接工具、不写会话窗口
- [x] 7.2 改写指令实现 prompt-injection 防护（把用户文本当数据），加入长度上限与空输入短路
- [x] 7.3 在 `cmd/server/main.go` 注册 `POST /api/session/{id}/prompt/optimize`，请求 `{text}` → 响应 `{optimized}`，模型不可用返回 502
- [x] 7.4 表驱动单测：正常改写、空输入、注入文本被当数据处理、模型错误路径
- [x] 7.5 `go test ./...` 与 `go vet ./...` 通过

## 8. 提示词优化前端入口（需求：提示词优化入口）
- [x] 8.1 输入区新增「优化提示词」小按钮，调用端点并将结果回填输入框（不自动发送）
- [x] 8.2 处理空输入（不发请求）与失败（toast 提示 + 保留原文）
- [x] 8.3 验证：口语化输入 → 优化 → 回填 → 用户确认后发送整链路可用（浏览器实测通过）

## 9. 收尾验证
- [x] 9.1 全流程冒烟（healthz/platforms/index/app.js/styles.css/session/optimize）+ 浏览器实测优化按钮与布局
- [x] 9.2 `openspec validate polish-studio-ui --strict` 通过
