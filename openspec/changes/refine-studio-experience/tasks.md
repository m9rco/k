# Tasks: 体验第一次优化专项

## 1. 后端：真流式与思考增量（需求1）
- [ ] 1.1 `chatmodel.go`：OpenAI 兼容分支改为 `stream:true`，解析 SSE `delta.content` 与 `delta.reasoning_content` 增量
- [ ] 1.2 `chatmodel.go`：Anthropic 分支改为 messages 流式，解析 `content_block_delta` 的 `text`/`thinking`
- [ ] 1.3 `chatmodel.go`：流式解析失败时降级为整体读取后补发（不空屏）
- [ ] 1.4 `agent.go`：流式循环分别 emit 回答增量与 `reasoning` 增量
- [ ] 1.5 两家 provider 各加流式契约测试 + 降级路径测试

## 2. 后端：二次调整尺寸继承（需求12）
- [ ] 2.1 `generation/service.go`：`SourceAssetID` 非空时取源资产宽高写入 `Request.Width/Height`
- [ ] 2.2 产物落库记录实际宽高（`AssetRecord.Width/Height`）
- [ ] 2.3 单测：二次调整产物尺寸贴近源图、非默认 1024²

## 3. 后端：zip 分目录（需求9）
- [ ] 3.1 确认/补齐裁剪产物落库时的渠道与尺寸来源元数据（design Open Question）
- [ ] 3.2 `download`：打包按 `渠道/尺寸` 组织目录，无归属资产入兜底目录
- [ ] 3.3 单测：分目录结构正确、兜底目录生效、跳过无效项仍工作

## 4. 前端：布局与目标一致性（需求11、3）
- [ ] 4.1 `styles.css`：主布局改为工作区:对话 = 3:7，对话区为主区域；窄屏堆叠
- [ ] 4.2 `app.js`：资产卡片所有操作闭包显式捕获自身 assetId，移除对隐式活动资产的依赖
- [ ] 4.3 浏览器手测：选第 N 张、操作命中第 N 张（右键/二次调整/下载/切尺寸）

## 5. 前端：思考折叠与工具状态卡（需求1、4）
- [ ] 5.1 思考增量进入"思考中…"可展开容器并逐字追加
- [ ] 5.2 回答开始/工具调用开始时自动折叠思考块，保留展开入口
- [ ] 5.3 工具调用卡片严格呈现 待执行/执行中/成功/失败 + 结果摘要，杜绝无结局态
- [ ] 5.4 无思考内容时不渲染空思考块

## 6. 前端：偏好关键词与路线图（需求5、6）
- [ ] 6.1 发送消息时做停用词过滤 + 高频关键词统计，累计到前端状态
- [ ] 6.2 偏好角落渲染关键词；无记录时 `hidden`
- [ ] 6.3 路线图区域以"规划中"卡片展示自学习/团队知识库/生视频等方向（仅展示）

## 7. 前端：批量切尺寸与选择器全选（需求10、9）
- [ ] 7.1 尺寸选择器加"全选当前渠道可裁剪尺寸"（排除 producible=false）
- [ ] 7.2 工作区多选后新增"批量切尺寸"入口，与"打包下载"并存
- [ ] 7.3 批量切尺寸对所选资产逐一发起裁剪并回填

## 8. 前端：流畅度与品牌科技感（需求0、2、7、8）
- [ ] 8.1 流式追加时滚动锚定、消除抖动
- [ ] 8.2 工作区更新用过渡/骨架，避免空白闪烁
- [ ] 8.3 长任务占位卡阶段化进度反馈，任务进行中其他操作不阻塞
- [ ] 8.4 统一视觉语言（配色/圆角/间距/动效节奏），强化首屏品牌与服务定位
- [ ] 8.5 确认异常均以 toast 小弹窗呈现（验收基线）

## 9. 验证
- [ ] 9.1 `go build ./...` + `go test ./...` 全绿
- [ ] 9.2 浏览器端到端回归：真流式思考折叠、3:7 布局、目标一致性、全选+批量切尺寸、分目录 zip、尺寸继承、偏好角落、路线图卡
- [ ] 9.3 `openspec validate refine-studio-experience --strict` 通过
