# Secrets — never commit real credentials.
.env
.env.local
*.local

# Local data / generated artifacts (db, generated assets, runtime logs).
/data/

# Build output.
/server
/bin/
*.out

# OS / editor cruft.
.DS_Store

# Playwright MCP browser-test artifacts (snapshots, console logs).
.playwright-mcp/

# Claude Code worktree artifacts
.claude/worktrees/
